import React from "react";
import { Typography } from "@mui/material";

const ExamCheckTitle: React.FC = () => {
    return (
        <Typography
            variant="h4"
            align="center"
            gutterBottom
            fontWeight="bold"
        >
            בדיקת מבחנים
        </Typography>
    );
};

export default ExamCheckTitle;
